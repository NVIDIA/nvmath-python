# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
#
# SPDX-License-Identifier: Apache-2.0

# TODO(nvmath-python CI): Implement the nvmath-python docs build.
#
# The previous contents of this file were copied from cuda-python and built
# separate Sphinx sites for cuda.pathfinder, cuda.bindings, cuda.core, and the
# cuda-python meta package. It referenced a `cuda-python-docs` conda env at
# `cuda_python/docs/environment-docs.yml`, neither of which exist in
# nvmath-python.
#
# What this workflow needs to do (follow-up implementation):
#   1. Check out the repo and set up a Python environment capable of building
#      the Sphinx site rooted at `docs/sphinx/` (conf.py, index.rst, etc.).
#      The conda env / docs extra does not yet exist — decide whether to
#      create `docs/sphinx/environment.yml` or a `docs` extra in pyproject.toml.
#   2. Install nvmath-python into that environment so autodoc can import it.
#   3. `sphinx-build` `docs/sphinx/` into a site directory.
#   4. On PR events, publish to a PR-preview folder via .github/actions/doc_preview.
#      The PR-preview action has already been updated to reference nvmath-python
#      (single-package URL, nvmath-python-bot identity).
#   5. On release tag events (`is-release: true`), publish the site under the
#      correct version folder (e.g., /latest/ and /v0.8.0/).
#
# Until this is implemented, the job below exits successfully with a TODO
# message so the ci.yml dependency graph remains valid.

name: "CI: Build and update docs"

on:
  workflow_call:
    inputs:
      is-release:
        description: "Are we building release docs?"
        required: false
        default: false
        type: boolean

jobs:
  build:
    name: Build docs (TODO)
    if: ${{ github.repository_owner == 'NVIDIA' && !cancelled() }}
    runs-on: ubuntu-latest
    steps:
      - name: TODO — implement nvmath-python docs build
        run: |
          echo "::warning::build-docs.yml is a stub. Implement the nvmath-python Sphinx build."
          echo "is-release: ${{ inputs.is-release }}"
